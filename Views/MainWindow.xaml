<Window x:Class="EZFileStateHandler.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EZFileStateHandler" xmlns:usercontrols="clr-namespace:EZFileStateHandler.Views.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="#333" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition  />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <usercontrols:MenuBar Grid.Column="0" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <usercontrols:StateHandlerMaker Grid.Column="0" />
            <usercontrols:ProfileViewer Grid.Column="1" />
        </Grid>
    </Grid>
</Window>
